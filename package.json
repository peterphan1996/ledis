{
  "name": "demoreactapp",
  "version": "1.0.0",
  "description": "React App",
  "main": "index.js",
  "scripts": {
    "heroku-postbuild": "cd client && yarn install && yarn build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "yarn heroku-postbuild && node server.js"
  },
  "repository": {
    "type": "git",
    "url": "\\"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "nodemon": "^2.0.13"
  }
}
